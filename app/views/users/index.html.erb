<%= render "layouts/header" %>

<center><h1>TRAVLR.</h1>
  <h3>Travlr's most travelled!</h3>
   <% User.number_of_trips.each.with_index(1) do |user, index| %>
    <p> <%= index %>.<%= user.username.capitalize %> - <%= user.trips.count %> trips!</p>
     <% end %>
  <br>

<br>
  <%= render "layouts/footer" %>
